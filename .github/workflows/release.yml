name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  #TODO:
  #   1. Register a package at https://openupm.com/packages/add/
  #   2. Uncomment this job
  #   3. Push a v.X.Y.Z tag; will be automatically picked up
  # publish-openupm:
  #   name: Publish to OpenUPM
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Create GitHub Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         generate_release_notes: true

  #TODO:
  # pack-tarball:
  #   name: Pack & Attach Tarball
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #
  #     - name: Pack UPM tarball
  #       working-directory: Packages/com.xx.easyaccessibility
  #       run: npm pack
  #
  #     - name: Attach tarball to GitHub Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         files: Packages/com.xx.easyaccessibility/*.tgz
